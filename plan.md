# 점검시트 디지털화 시스템 — 기획/설계 (plan.md)

> 종이로 작성하던 현장 점검시트를 태블릿 기반 프로그램으로 전환하는 프로젝트.

---

## 1. 프로젝트 개요

회사 내 다양한 점검시트를 종이 대신 태블릿으로 작성하고, 사내 서버에 저장하는 시스템.

- **여러 종류의 점검시트를 생성·관리**할 수 있다. (핵심)
- 한 태블릿에서 **필요한 점검시트를 불러와** 현장에서 점검·입력한다.
- WiFi가 자주 끊기므로 **오프라인에서도 입력/저장**되고, 연결되면 서버로 자동 동기화된다.
- 점검시트별로 수신자(메일)를 등록할 수 있다. *(메일 발송 기능은 후속 단계에서 구현)*

---

## 2. 핵심 요구사항

| # | 요구사항 | 상태 |
|---|---------|------|
| 1 | 태블릿에서 점검시트를 쉽게 만들 수 있음 (노코드 빌더) | 포함 |
| 2 | **여러 점검시트 생성·관리, 태블릿에서 골라 불러와 점검** | 포함 (핵심) |
| 3 | WiFi 불안정 → 오프라인 저장 후 자동 동기화 | 포함 |
| 4 | 점검시트별 수신자 등록 | 포함 (등록 UI) |
| 5 | 지정 메일로 결과 자동 발송 | **후속 단계로 보류** |
| 6 | 사내 PC 서버(인터넷 차단, 사내망 전용)에서 운영 | 전제 |

---

## 3. 운영 환경 전제

- **서버**: 사내 PC 1대. **인터넷 차단, 사내망(LAN/WiFi)만 연결.**
- **태블릿**: 안드로이드 중심(기기 미정) → 기기 비종속 **PWA(설치형 웹앱)**로 구현하여 어떤 태블릿에서도 동작.
- **네트워크**: 사내 WiFi, 연결이 자주 끊김 → 오프라인 우선(Offline-first) 설계 필수.
- **메일**: 서버가 인터넷이 없으므로 추후 **사내 메일서버(SMTP 릴레이)** 연동으로 구현 예정. 현재 단계에서는 수신자 "등록"까지만 만들고 발송은 보류.

---

## 4. 시스템 아키텍처

```
┌──────────────── 사내망 (인터넷 차단) ────────────────┐
│                                                      │
│   [안드로이드 태블릿들] ──WiFi(불안정)──┐            │
│    · PWA 앱                            │            │
│    · IndexedDB 로컬저장                │            │
│    · 동기화 대기열                     ▼            │
│                            ┌────────────────────┐   │
│                            │   사내 PC 서버      │   │
│                            │  ┌──────────────┐  │   │
│                            │  │ 웹서버(API+UI)│  │   │
│                            │  ├──────────────┤  │   │
│                            │  │ DB           │  │   │
│                            │  │ (점검시트/    │  │   │
│                            │  │  결과/수신자) │  │   │
│                            │  └──────────────┘  │   │
│                            └────────────────────┘   │
│                       (메일서버 연동은 후속 단계)    │
└──────────────────────────────────────────────────────┘
```

---

## 5. 기술 스택

| 영역 | 기술 | 선택 이유 |
|------|------|----------|
| 태블릿 앱 | **PWA** (React + TypeScript) | 앱스토어 불필요, 사내 주소 접속, 오프라인 기본 지원, 기기 비종속 |
| 로컬 저장 | IndexedDB | 오프라인 시 태블릿에 안전 보관 |
| 오프라인 동기화 | Service Worker + 동기화 큐 | WiFi 복귀 감지 시 자동 전송 |
| 백엔드 | Node.js + Express | 가볍고 사내 PC 운영 용이 |
| 데이터베이스 | PostgreSQL (초기엔 SQLite도 가능) | 점검 이력 안정적 보관 |
| 배포 | Docker Compose | 사내 PC 1대에 일괄 설치 |
| 메일(후속) | Nodemailer → 사내 SMTP 릴레이 | 인터넷 없는 서버 대응 |

---

## 6. 주요 기능 모듈

### A. 점검시트 템플릿 관리 (핵심)
- 여러 점검시트 **목록 조회 / 신규 생성 / 수정 / 복제 / 비활성화**
- 노코드 빌더: 제목, 설명, 점검 항목 추가·삭제·순서변경
- 항목 유형: 체크(OK/NG), 숫자입력, 텍스트, 날짜, 선택목록, (사진은 후속)
- 항목별 합격기준(예: 측정 범위) 설정
- 시트별 **수신자(메일) 등록** (발송은 후속)
- 템플릿 버전 관리 (수정해도 과거 점검 결과는 그대로 보존)

### B. 점검 실행 (현장)
- 태블릿에서 **사용할 점검시트를 목록에서 선택해 불러오기**
- 오프라인에서도 입력·임시저장 정상 동작
- 제출 시 로컬 저장 → 동기화 대기열 등록
- 상단에 온라인/오프라인·동기화 대기 건수 표시

### C. 오프라인 동기화 엔진
- WiFi 복귀 감지 → 미전송 결과 순차 업로드
- 고유 ID로 중복 방지, 실패 시 자동 재시도
- 태블릿 → 서버 단방향 동기화 우선

### D. 결과 조회
- 관리자 웹에서 점검 결과 목록/검색/상세 조회
- 내보내기(Excel/PDF)는 후속 단계

### E. 메일 발송 (후속 단계)
- 사내 메일서버 연동 후, 점검 완료 시 등록된 수신자에게 결과 발송

---

## 7. 데이터 모델 (개략)

```
ChecklistTemplate (점검시트 템플릿)
 ├─ id, 제목, 설명, 버전, 활성여부, 생성일시
 ├─ fields: 점검 항목 목록
 │    └─ 유형, 라벨, 필수여부, 합격기준, 순서
 └─ recipients: 수신자 이메일 목록  (발송은 후속)

Inspection (점검 결과)
 ├─ id (태블릿에서 생성하는 고유 UUID — 중복 동기화 방지)
 ├─ templateId, templateVersion
 ├─ 작성자, 작성일시
 ├─ answers: 항목별 입력값
 └─ syncStatus: 대기 / 완료
```

---

## 8. 개발 단계 (로드맵)

| 단계 | 내용 | 목표 |
|------|------|------|
| **0. 골격** | 프로젝트 구조, DB, 서버/PWA 기본 세팅 | 개발 기반 |
| **1. 템플릿 관리** | 점검시트 생성/목록/수정/복제 (빌더) | 여러 시트 관리 |
| **2. 점검 실행** | 시트 불러오기 + 입력 + 서버 저장 | 온라인 점검 |
| **3. 오프라인 동기화** | IndexedDB 저장 + 자동 동기화 (PWA 핵심) | WiFi 불안정 대응 |
| **4. 수신자 등록** | 시트별 수신자 관리 UI | 메일 준비 |
| **5. 결과 조회** | 결과 목록/검색/상세 | 활용 |
| **6. (후속) 메일 발송** | 사내 SMTP 릴레이 연동 발송 | 자동 통보 |

---

## 9. 보류/후속 결정 사항

- 사내 메일서버(SMTP 릴레이) 존재 여부 확인 → 메일 단계에서 확정
- 사진 첨부 기능 필요 여부
- 사용자 로그인/권한 관리 필요 여부
- 결과 PDF/Excel 내보내기 형식
