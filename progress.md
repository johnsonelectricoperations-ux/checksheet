# 진행 현황 (progress.md)

> 프로젝트 진행 상황을 기록하는 파일. 작업이 진행될 때마다 업데이트한다.

---

## 현재 상태

- **현재 단계**: 3단계 완료 → 4단계(수신자 등록) 준비
- **브랜치**: `claude/epic-brown-v38qr4`
- **최종 업데이트**: 2026-06-19

---

## 단계별 진행률

| 단계 | 내용 | 상태 |
|------|------|------|
| 기획/설계 | 시스템 구상, 요구사항 정리 | ✅ 완료 |
| 문서화 | plan.md / progress.md / task.md 작성 | ✅ 완료 |
| 0. 골격 | 프로젝트 구조, DB, 서버/PWA 세팅 | ✅ 완료 |
| 1. 템플릿 관리 | 점검시트 생성/목록/수정/복제 | ✅ 완료 |
| 2. 점검 실행 | 시트 불러오기 + 입력 + 저장 | ✅ 완료 |
| 3. 오프라인 동기화 | IndexedDB + 자동 동기화 | ✅ 완료 |
| 4. 수신자 등록 | 시트별 수신자 관리 | ⬜ 대기 |
| 5. 결과 조회 | 결과 목록/검색/상세 | ⬜ 대기 |
| 6. (후속) 메일 발송 | 사내 SMTP 릴레이 연동 | ⬜ 보류 |

상태 표기: ⬜ 대기 / 🟡 진행중 / ✅ 완료 / ⏸️ 보류

---

## 작업 로그

### 2026-06-19
- 시스템 요구사항 정리 및 아키텍처 구상 완료
- 주요 결정사항:
  - 태블릿 앱은 **PWA**로 구현 (안드로이드 중심, 기기 미정 → 기기 비종속)
  - 서버는 **사내망 전용(인터넷 차단)** 전제
  - **메일 발송 기능은 후속 단계로 보류**, 현 단계는 수신자 등록까지
  - 개발 범위에 **오프라인 동기화 포함**
  - 여러 점검시트를 생성·관리하고 태블릿에서 골라 불러오는 구조
- `plan.md`, `progress.md`, `task.md` 작성
- 추가 요구: **항목별 사진·동영상 촬영·저장** 기능 반영
  - 미디어 항목 유형 추가, 미디어는 IndexedDB(Blob) 로컬 저장
  - 오프라인 동기화 시 미디어는 텍스트와 분리해 별도 업로드(청크/재시도)
  - 서버는 파일 스토리지에 저장, DB에는 메타데이터만 보관

---

### 2026-06-19 (0단계)
- 프로젝트 골격 구축 완료
  - `server/`: Node.js + Express + SQLite. 헬스체크(`/api/health`) 동작 확인
  - DB 스키마: templates / inspections / media_files (사진·동영상 메타 포함)
  - `web/`: React + TS + Vite PWA. 온라인/오프라인 상태 표시, 서비스워커 생성 확인
  - Docker Compose + 각 Dockerfile + nginx 프록시 구성
  - 루트 README 작성
- 빌드/실행 검증 완료 (server build+run, web build OK)

### 2026-06-19 (1단계)
- 점검시트 템플릿 관리 완료
  - 백엔드: `/api/templates` CRUD (목록/조회/생성/수정/복제/활성토글)
    - 입력 검증, 수정 시 version 자동 증가
  - 프론트: 템플릿 목록 화면 + 노코드 빌더(TemplateEditor)
    - 항목 추가/삭제/순서이동, 7개 항목 유형
    - 숫자 합격기준(min/max/단위), select 선택지, 미디어 개수/길이 옵션
    - 수신자 등록 입력(발송은 후속)
  - API 흐름 검증 완료 (생성→수정 v2→복제→비활성 필터링)

### 2026-06-19 (2단계)
- 점검 실행 기능 완료
  - 백엔드: `/api/inspections` 저장(idempotent, 같은 id 덮어쓰기)/목록/조회
    - `/api/inspections/:id/media` 미디어 multipart 업로드 (multer, 디스크 저장)
    - `/api/media/:id` 저장된 미디어 제공
  - 프론트: 목록의 "점검 시작" → InspectionForm
    - 유형별 입력 UI (체크 OK/NG 버튼, 숫자+기준힌트, 텍스트, 날짜, 선택)
    - MediaCapture 컴포넌트: capture 속성으로 카메라 연동, 미리보기/삭제
    - 검증(필수/숫자기준/미디어 최소개수) → 결과 저장 후 미디어 분리 업로드
  - API 흐름 검증 완료 (저장→재저장 idempotent→미디어 업로드→조회)
- 비고: 점검시트 생성/관리는 웹앱이라 PC 브라우저에서도 동일하게 사용 가능

### 2026-06-19 (3단계)
- 오프라인 저장 + 자동 동기화 완료 (PWA 핵심)
  - `idb` 도입, IndexedDB 스토어: templates(캐시) / pendingInspections(대기열)
  - 오프라인 우선: 점검 제출 시 결과+미디어(Blob)를 먼저 로컬 저장
  - 동기화 엔진(sync.ts): 본문 idempotent 저장 → 미디어 파일 단위 업로드
    → 완료 항목 대기열 제거. 실패 항목은 다음 기회 재시도(부분 동기화 추적)
  - 자동 트리거: online 이벤트 + 30초 주기 폴링 + 제출 직후
  - 템플릿 목록/점검화면 오프라인 폴백(캐시) 적용
  - 헤더에 대기 건수/동기화중 표시, 탭하면 즉시 동기화
- 검증: 웹 빌드/타입 통과. 서버측 idempotent 저장·분리 업로드 경로는 2단계 실측 완료
  (브라우저 IndexedDB 실측은 후속 통합 테스트 단계에서 보강 예정)

---

## 다음 할 일

- [ ] 4단계: 수신자 등록 화면 정리 (현재 빌더에 입력란 존재 → 별도 관리/검증)
