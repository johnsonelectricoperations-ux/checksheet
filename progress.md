# 진행 현황 (progress.md)

> 프로젝트 진행 상황을 기록하는 파일. 작업이 진행될 때마다 업데이트한다.

---

## 현재 상태

- **현재 단계**: 운영 전 보완 — A~E 완료. F(실기기)는 실제 태블릿 필요 → 사용자 확인 대기
- **브랜치**: `claude/epic-brown-v38qr4`
- **최종 업데이트**: 2026-06-19

---

## 단계별 진행률

| 단계 | 내용 | 상태 |
|------|------|------|
| 기획/설계 | 시스템 구상, 요구사항 정리 | ✅ 완료 |
| 문서화 | plan.md / progress.md / task.md 작성 | ✅ 완료 |
| 0. 골격 | 프로젝트 구조, DB, 서버/PWA 세팅 | ✅ 완료 |
| 1. 템플릿 관리 | 점검시트 생성/목록/수정/복제 | ✅ 완료 |
| 2. 점검 실행 | 시트 불러오기 + 입력 + 저장 | ✅ 완료 |
| 3. 오프라인 동기화 | IndexedDB + 자동 동기화 | ✅ 완료 |
| 4. 수신자 등록 | 시트별 수신자 관리 | ✅ 완료 |
| 5. 결과 조회 | 결과 목록/검색/상세 | ✅ 완료 |
| A. HTTPS | 자체서명 인증서 + nginx TLS | ✅ 완료 |
| B. 로그인/권한 | 관리자/작업자 구분 | ✅ 완료 |
| C. 결과 스냅샷 | 항목 정의 스냅샷 저장 | ✅ 완료 |
| D. 사진 압축 | 업로드 전 클라이언트 압축 | ✅ 완료 |
| E. 동영상 길이 제한 | duration 검증 | ✅ 완료 |
| F. 실기기 테스트 | 오프라인 동기화 실측 | ⬜ 대기 |
| G. 내보내기 | CSV + 인쇄/PDF | ✅ 완료 |
| H. 대시보드 강화 | NG요약/판정 | ✅ 완료 |
| I. 백업 | DB·미디어 백업 절차 | ⬜ 대기 |
| 6. (후속) 메일 발송 | 사내 SMTP 릴레이 연동 | ⬜ 보류 |

상태 표기: ⬜ 대기 / 🟡 진행중 / ✅ 완료 / ⏸️ 보류

---

## 작업 로그

### 2026-06-19
- 시스템 요구사항 정리 및 아키텍처 구상 완료
- 주요 결정사항:
  - 태블릿 앱은 **PWA**로 구현 (안드로이드 중심, 기기 미정 → 기기 비종속)
  - 서버는 **사내망 전용(인터넷 차단)** 전제
  - **메일 발송 기능은 후속 단계로 보류**, 현 단계는 수신자 등록까지
  - 개발 범위에 **오프라인 동기화 포함**
  - 여러 점검시트를 생성·관리하고 태블릿에서 골라 불러오는 구조
- `plan.md`, `progress.md`, `task.md` 작성
- 추가 요구: **항목별 사진·동영상 촬영·저장** 기능 반영
  - 미디어 항목 유형 추가, 미디어는 IndexedDB(Blob) 로컬 저장
  - 오프라인 동기화 시 미디어는 텍스트와 분리해 별도 업로드(청크/재시도)
  - 서버는 파일 스토리지에 저장, DB에는 메타데이터만 보관

---

### 2026-06-19 (0단계)
- 프로젝트 골격 구축 완료
  - `server/`: Node.js + Express + SQLite. 헬스체크(`/api/health`) 동작 확인
  - DB 스키마: templates / inspections / media_files (사진·동영상 메타 포함)
  - `web/`: React + TS + Vite PWA. 온라인/오프라인 상태 표시, 서비스워커 생성 확인
  - Docker Compose + 각 Dockerfile + nginx 프록시 구성
  - 루트 README 작성
- 빌드/실행 검증 완료 (server build+run, web build OK)

### 2026-06-19 (1단계)
- 점검시트 템플릿 관리 완료
  - 백엔드: `/api/templates` CRUD (목록/조회/생성/수정/복제/활성토글)
    - 입력 검증, 수정 시 version 자동 증가
  - 프론트: 템플릿 목록 화면 + 노코드 빌더(TemplateEditor)
    - 항목 추가/삭제/순서이동, 7개 항목 유형
    - 숫자 합격기준(min/max/단위), select 선택지, 미디어 개수/길이 옵션
    - 수신자 등록 입력(발송은 후속)
  - API 흐름 검증 완료 (생성→수정 v2→복제→비활성 필터링)

### 2026-06-19 (2단계)
- 점검 실행 기능 완료
  - 백엔드: `/api/inspections` 저장(idempotent, 같은 id 덮어쓰기)/목록/조회
    - `/api/inspections/:id/media` 미디어 multipart 업로드 (multer, 디스크 저장)
    - `/api/media/:id` 저장된 미디어 제공
  - 프론트: 목록의 "점검 시작" → InspectionForm
    - 유형별 입력 UI (체크 OK/NG 버튼, 숫자+기준힌트, 텍스트, 날짜, 선택)
    - MediaCapture 컴포넌트: capture 속성으로 카메라 연동, 미리보기/삭제
    - 검증(필수/숫자기준/미디어 최소개수) → 결과 저장 후 미디어 분리 업로드
  - API 흐름 검증 완료 (저장→재저장 idempotent→미디어 업로드→조회)
- 비고: 점검시트 생성/관리는 웹앱이라 PC 브라우저에서도 동일하게 사용 가능

### 2026-06-19 (3단계)
- 오프라인 저장 + 자동 동기화 완료 (PWA 핵심)
  - `idb` 도입, IndexedDB 스토어: templates(캐시) / pendingInspections(대기열)
  - 오프라인 우선: 점검 제출 시 결과+미디어(Blob)를 먼저 로컬 저장
  - 동기화 엔진(sync.ts): 본문 idempotent 저장 → 미디어 파일 단위 업로드
    → 완료 항목 대기열 제거. 실패 항목은 다음 기회 재시도(부분 동기화 추적)
  - 자동 트리거: online 이벤트 + 30초 주기 폴링 + 제출 직후
  - 템플릿 목록/점검화면 오프라인 폴백(캐시) 적용
  - 헤더에 대기 건수/동기화중 표시, 탭하면 즉시 동기화
- 검증: 웹 빌드/타입 통과. 서버측 idempotent 저장·분리 업로드 경로는 2단계 실측 완료
  (브라우저 IndexedDB 실측은 후속 통합 테스트 단계에서 보강 예정)

### 2026-06-19 (보완 H: 대시보드 강화)
- 합격/불합격 자동 판정(evaluate 유틸: 체크 NG, 숫자 기준 위반)
  - 결과 목록: 판정 배지 + 합격/불합격 요약 카운트 + 판정 필터
  - 결과 상세: 종합 판정 배너 + 불합격 항목 빨강 강조
  - 판정 기준은 스냅샷 항목 정의 우선

### 2026-06-19 (보완 G: 결과 내보내기)
- CSV 내보내기(Excel 호환, BOM): 시트 선택 시 항목 라벨을 열로, 미디어는 첨부수
- 결과 상세 인쇄/PDF 저장(print 스타일시트로 헤더/버튼 숨김) — 무의존

### 2026-06-19 (보완 E: 동영상 길이 제한)
- 동영상 촬영 후 duration 검증, maxDurationSec 초과 시 거부 + 안내 메시지
  - getVideoDuration 유틸(메타데이터 로드), 촬영 버튼에 최대 길이 힌트

### 2026-06-19 (보완 D: 사진 자동 압축)
- 업로드 전 클라이언트 이미지 압축(canvas, 최대 1600px, JPEG q0.7)
  - compressImage 유틸 + 점검사진/선택첨부/참조사진에 적용, 동영상 제외
  - 실패하거나 더 커지면 원본 유지(폴백)
  - 빌드/타입 통과(런타임은 브라우저 canvas 필요)

### 2026-06-19 (보완 C: 결과 무결성 스냅샷)
- 점검 저장 시 템플릿 항목 정의 스냅샷(title/version/fields) 보관
  - inspections.template_snapshot_json 컬럼 + 경량 마이그레이션(ensureColumn)
  - 클라이언트가 본 스냅샷 전송, 미전달 시 서버가 현재 템플릿으로 폴백
  - ResultDetail 은 스냅샷 우선 사용 → 템플릿 수정과 무관하게 과거 결과 정확
  - 실측: 템플릿 v2 로 수정 후에도 과거 점검 결과는 원본 라벨/제목 유지

### 2026-06-19 (보완 B: 로그인/권한)
- 토큰 기반 로그인 + 역할(admin/worker) 구현
  - 백엔드: users/sessions 테이블, scrypt 해싱(무의존), requireAuth/requireAdmin
    - /api/auth(login/logout/me), /api/users(관리자), 기본 admin 시드
    - 템플릿 변경/참조파일 업로드는 admin 전용, 조회/점검은 인증 사용자
    - 미디어 GET 은 헤더 못 보내는 <img> 위해 ?token= 쿼리도 허용
  - 프론트: 세션(localStorage), Login 화면, useAuth, 토큰 자동 부착
    - 미인증 시 로그인 화면, 401 시 세션 정리
    - 작업자는 템플릿 편집 UI 숨김, 점검자 이름 로그인 사용자로 프리필
    - 헤더에 사용자/로그아웃
  - 검증: 401/403/201 권한 흐름, 미디어 ?token= 200/401 실측 완료

### 2026-06-19 (보완 A: HTTPS)
- 자체 서명 인증서 + nginx HTTPS 구성 (오프라인 동작 전제 해결)
  - scripts/generate-cert.sh: 서버IP/호스트명으로 SAN 포함 인증서 생성 (검증 완료)
  - nginx: 443 SSL 서비스 + 80→443 리다이렉트, 업로드 200m
  - docker-compose: certs 볼륨 마운트, web 8443 노출, server는 expose(평문 API 차단)
  - README: 인증서 생성/실행/태블릿 신뢰 등록 안내 추가
  - docker compose config 검증 통과

### 2026-06-19 (5단계)
- 결과 조회 대시보드 완료
  - ResultsList: 점검 결과 목록, 시트별 필터 + 점검자/시트명 검색, 미디어 개수 표시
  - ResultDetail: 항목 라벨별 입력값 + 사진/동영상 열람(클릭 시 원본)
  - 헤더 내비게이션(점검시트/결과) 추가
- 검증: 목록/상세 API 데이터 형태 실측(미디어 개수, answers, fieldId 매핑)
- 참고: 상세 라벨은 현재 템플릿 기준. 템플릿 수정 시 과거결과 라벨 정확도를 위해
  향후 점검 시점의 필드 스냅샷 저장을 검토(후속 개선 항목)

### 2026-06-19 (추가: 항목 설명 + 참조 미디어)
- 템플릿 항목에 설명(텍스트) + 참조용 사진/동영상 등록, 점검 시 참고 표시
  - 백엔드: 범용 파일 저장소 `files` 테이블 + `/api/files` 업로드/제공
  - Field 에 description, guideMedia[{id,type}] 추가 (JSON 저장)
  - 프론트: GuideMediaEditor(빌더, 즉시 업로드), GuideView(점검 화면 참고)
  - PWA runtimeCaching 으로 참조 미디어 1회 열람 후 오프라인 열람 가능
- 검증: 파일 업로드→제공(200), 템플릿에 desc+guideMedia 왕복 저장 실측

### 2026-06-19 (추가: 선택적 첨부)
- 점검 입력 시 모든 항목에 필요할 때만 사진/동영상 첨부 가능
  - OptionalAttachments 컴포넌트 (📎 토글 → 📷/🎥 버튼)
  - 사진/동영상 유형 항목은 기존 MediaCapture 유지, 그 외 항목엔 선택 첨부
  - 백엔드 변경 없음(미디어는 이미 fieldId+type 단위 저장), 동기화 경로 그대로 사용

### 2026-06-19 (4단계)
- 수신자 등록 완료
  - RecipientsEditor 컴포넌트: 칩 형태 추가/삭제, Enter/쉼표로 다중 입력
  - 이메일 형식 검증(프론트) + 서버 검증(잘못된 형식 400), 중복 자동 제거
  - 템플릿에 recipients 포함 저장 (기존 구조 활용)
- 검증: 웹 빌드 통과, 서버 유효/무효 이메일 응답 실측(201 / 400)

---

## 다음 할 일

- [ ] 5단계: 결과 조회 (점검 결과 목록/검색/상세 + 미디어 보기)
